# NeriPlayer Win · TODO 蓝图

> 本文只描述「要什么」,不约定「怎么做」。实现方案在各里程碑开工时再定。

## 目标

Windows 桌面音乐播放器,自用优先:网易云 + B站两个音源,
扫码登录、读取收藏/歌单、在线播放。
轻量是硬要求:启动快、常驻内存低、不引入重型依赖。

## 里程碑

### M0 · 骨架
- [x] 仓库布局:本项目在外层,Android 参考实现收进 `reference/`(不参与构建)
- [x] Python 项目骨架:可安装、可启动的主窗口(侧栏 + 歌曲列表 + 播放条占位)
- [x] 依赖一键安装后窗口可正常启动(本机验证,uv + 离屏冒烟)

### M1 · 网易云闭环(代码完成,待真机验证)
- [x] 网易云扫码登录:二维码展示在窗口内,手机扫码确认
- [x] 登录态持久化:重启免扫码;过期可感知并提示重新登录
- [x] 读取「我喜欢的音乐」与自建歌单列表
- [x] 歌单展开为歌曲列表(标题 / 歌手 / 时长)
- [x] 点歌播放:解析播放地址交给播放内核
- [x] 播放控制:播放/暂停、上一首/下一首、进度拖动、音量
- [x] 真机端到端验证:登录 → 歌单加载 → 出声播放(2026-09-14 用户确认;实际登录方式为内嵌网页登录,扫码直连因桌面端指纹缺失被服务端拒绝)

### M2 · B站闭环(代码完成,待真机验证)
- [x] B站登录与登录态持久化(网页登录复用泛化后的 WebLoginDialog;
      扫码直连客户端留 API + 测试备用,UI 主路径与网易云同策略)
- [x] 读取收藏夹(以音频/歌单视角呈现)
- [x] B站点歌播放,播放控制体验与 M1 对齐(统一队列,混排可连续播)
- [x] 真机端到端验证:B站登录 → 收藏夹加载 → 出声播放(2026-09-14 用户确认)

### M3 · 播放体验与队列(2026-09-14 用户真机验证通过)
- [x] 播放队列:顺序 / 随机 / 单曲循环,队列展示与切跳
- [x] 一首播完自动接下一首(单曲循环重播当前;顺序到队尾即停;随机不立即重复)
- [x] 最小化到托盘;关闭行为可配置(直接退出 / 收进托盘,默认收进托盘)
- [x] 全局媒体键:播放/暂停、上一首/下一首(RegisterHotKey 线程热键,
      后台亦生效;被占用时回退 WM_APPCOMMAND 前台方案)

### M4 · 打磨与发布(达到「上线标准」时)
- [x] 打包为独立 exe:免装 Python / mpv 即可运行(Nuitka 单目录,tools/build_exe.py)
- [x] 性能验收:冷启动 ≤ 1.5s,常驻内存 ≤ 150MB 量级(实测 530ms / 141MB,docs/PERF.md)
- [x] 网络/登录/播放失败的友好提示,不裸崩(2026-09-15:播放失败改非阻塞
      提示+自动跳歌+连续失败熔断;网络错误走状态栏;登录失效走重登流程)
- [ ] 应用图标与 About;GPL-3.0 合规标注(衍生自 NeriPlayer)

### M5 · 计划中(README Roadmap 对应项;欢迎 Issue 补充)
- [x] 音质选择:手动指定播放音质并记忆偏好,播放时展示实际生效音质
- [x] 平台列表收起按钮:侧栏平台分区可折叠,歌单/收藏夹支持分区内拖拽排序
      (2026-09-15 实现:侧栏迁移为 QTreeWidget 三分区,折叠状态与排序持久化)
- [x] 播放失败自动跳下一首:失败提示改为非阻塞(状态栏+托盘气泡),
      自动接播下一首并设连续失败熔断;解析请求防卡死(可覆盖旧请求)
- [x] 后台播放体验:下一首播放地址预取(顺序/单曲模式)+ 进程优先级
      提升至 ABOVE_NORMAL(弱网与游戏场景降低切歌延迟)
      (2026-09-15 实现:起播后后台预取下一曲,切歌命中即零网络起播;
      预取过期有静默重解析兜底,切歌单/换模式即作废)
- [ ] 网易云推荐内容:每日推荐、私人 FM 等推荐源
      (每日推荐见下方 2026-09-17 条目;私人 FM 仍待做)
- [x] 「最近」列表:侧栏新增「最近」分区(与平台分区并列),记录最近**播放过**
      的歌单/收藏夹/稍后再看/每日推荐(仅浏览不记),栈式去重置顶,
      数量可在设置页调整(默认 8)
      (2026-09-17 实现:成功起播时压栈;浏览与播放队列解耦后,
      双击新列表起播或插播曲接播即视为「听过」;单测覆盖压栈/去重/上限/重启恢复)
- [x] 右键「下一首播放」:歌曲表右键插入当前曲之后,不替换整个队列;
      为此侧栏浏览不再重置播放队列(双击歌曲才切换队列来源)
      (2026-09-17 实现:PlayQueue.insert_next 按序累积插入,插播段
      随播过/跳过自动出队;随机模式历史索引同步修正;单测 8 例覆盖)
- [x] 网易云每日推荐歌曲:固定在「网易云 · 收藏」分区首位
      (2026-09-17 实现:/v3/discovery/recommend/songs,对照参考实现
      getDailyRecommendedSongs;响应按 firstSongArray 回退链解析;
      本机登录态离屏冒烟通过(33 首加载/入表,浏览不动队列),待真机验证)
- [x] 网易云收藏歌单:收藏的他人歌单在「网易云 · 收藏」分区与自建歌单并列,
      支持分区内拖拽排序
      (2026-09-15 实现:复用 user/playlist 响应按 subscribed 分流,语义对齐
      Android 参考 getUserSubscribedPlaylists;本机登录态离屏冒烟通过
      (39 个收藏歌单加载/展开),待真机验证)
- [x] B站「稍后再看」:并入「B站 · 收藏夹」分区首位(固定项,不参与拖拽排序)
      (2026-09-15 实现:toview/web 接口已用本机登录态实测可用(无 WBI 签名,
      参考实现无此接口故以实测定契约),列表映射为 BiliFavItem 复用收藏夹
      播放路径;本机登录态离屏冒烟通过(9 条加载/展开),待真机验证)
- [x] 歌曲表封面缩略图:序号列减半(默认 100px→50px),腾出的 50px 新增
      封面列(20px 圆角缩略图=播放条封面的 50% 缩放,固定行高 32px 防加载
      跳动);进表先全透明占位,表填完后按行序交给常驻 5 线程预取池并发
      拉取 96px CDN 缩略(与播放条共享磁盘缓存,互相加速),完成即逐行点亮,
      切表/清表即作废在途任务
      (2026-09-22 实现:CoverLoader 增内存缓存+原子写盘+预取池;
      单测 10 例覆盖并发上限/批次作废/缓存命中/表格接线;
      离屏验证缩略图精确居中,待真机验证)

- [x] 双源搜索:网易云 cloudsearch(weapi,type=1 单曲)+ B站 wbi
      search/type(视频);侧栏「最近」之上新增「搜索」入口(放大镜图标),
      主区歌曲表上方叠搜索框(半宽居中,两行高)与来源栏(网易云|B站
      各半宽),结果直接落现有歌曲表(缩略图/双击播放/右键插播全复用),
      支持分页「加载更多」,搜索结果可进「最近」
      (2026-09-23 实现:来源栏为 M3 主标签行风格(借鉴移动端
      PrimaryScrollableTabRow)——透明底+顶部滑动指示线动画;
      单测 17 例覆盖解析器/接线/最近回放;另修复同进程多窗口的
      mpv 事件线程泄漏与同主题重复 setStyleSheet 劣化,全量测试
      40 分钟级→90 秒;待真机验证)
- [x] 修复:进度条只能点击不能拖动(音量条正常)。根因是实现"点击任意
      位置跳转"时 monkeypatch mousePressEvent 整体吞掉了按下事件,
      QSlider 永不进入拖拽态(sliderPressed 也从不发出,拖动保护失效);
      改为 SeekSlider 子类自管 press/move/release,点击即跳、按住即拖、
      拖动中时间标签实时跟随且不被播放进度回写拉回
      (2026-09-23 实现:回归测试 4 例覆盖点击跳转/拖动取值/防回写/空曲目兜底)
- [x] 修复:暂停后(含切歌单)选新歌不自动播放,需手动暂停/播放才出声。
      根因是 mpv 的 pause 属性为全局、跨文件残留,play_url 未显式清除;
      顺带发现 UI 播放态与 mpv 实际暂停态失同步的表象由此而来
      (2026-09-23 实现:play_url 载入前强制 pause=False;libmpv 真驱回归)
- [x] 应用日志:RotatingFileHandler 写入数据目录 logs/app.log
      (%APPDATA%/neriplayer-win/,1MB×3 滚动);启动记录版本与环境代理快照
      getproxies(),B站/网易云/扫码网络失败记录 URL+异常+当时代理——
      定位远程用户"系统/环境代理残留导致 WinError 10061"类问题的关键
      证据;后台任务异常边界 logger.exception 落盘;未初始化静默、
      初始化失败绝不阻断启动
      (2026-09-23 实现:回归测试 4 例;随 v0.6.3 发布)
- [x] 代理失败降级直连:Bili/Netease 客户端各备一个 trust_env=False 的
      直连 httpx 客户端;主客户端(读系统/环境代理)出现连接类失败
      (ConnectError/ConnectTimeout,典型为代理残留的 WinError 10061)
      且存在代理配置时,直连重试一次;无代理配置不重试,重试失败抛
      原始异常(保留用户配置路径的报错语义);降级与降级失败均落日志
      (2026-09-23 实现;回归测试 6 例;随 v0.6.3 发布)
- [x] 界面中英双语:文案表收敛到 neriplayer_win/i18n.py(tr 键取,
      缺键兜底返回键名);设置页新增「语言 · Language」分组,内嵌
      自绘左右滑动滑块(中文|English,点选半区即切换,200ms 动画);
      默认中文,settings.json 增 language 键持久化(非法值回落 zh);
      切换即时全量重翻译(侧栏重建/表头/搜索页/播放条/托盘菜单/
      队列窗口/登录页,状态栏消息本就现算);「最近」栈里搜索条目的
      持久化标题保持规范前缀「搜索:」不随语言变(老数据回放不受
      影响),仅展示前缀随语言;设置页内容包 QScrollArea(分组增多后
      页面 minHint 不再顶高主窗口,默认窗高保持 680);更多语言
      (0.7 计划)只需在 i18n.py 扩充 VALID_LANGUAGES 与文案表
      (2026-09-25 实现;回归测试 26 例覆盖文案表/持久化/滑块交互/
      全量重翻译/双语回放/窗口高度回归;离屏+windows 双平台渲染经视觉验收)

- [x] Win 任务栏缩略图工具栏:鼠标悬停任务栏图标的窗口预览框底部
      上一首/播放暂停/下一首按钮,不点主窗即可控制播放(Windows
      ITaskbarList3 ThumbBar,ctypes 直调 COM,播放态图标/禁用态/
      tooltip 随状态与语言同步)
      (2026-09-26 实现:ui/taskbar.py,CoCreateInstance 拿 ITaskbarList3
      vtable 直调 ThumbBarAddButtons/Update;SVG 图标按系统明暗
      AppsUseLightTheme 染色经 CreateIconIndirect 转 HICON;按钮点击
      经 WM_COMMAND 原生过滤器转信号(与媒体键同套路);附加时机=
      showEvent 即试 + TaskbarButtonCreated 消息自动重试;真机 windows
      平台验证按钮注册成功;回归 7 例)
      (2026-09-26 修复真机不可见:① 托盘隐藏→重显后任务栏按钮重建、
      注册失效不重挂——TaskbarButtonCreated 现强制重加(Add 被拒回退
      Update),真机验证往返后附加保持;② 队列窗口 Qt.Window 与主窗
      分组,而分组预览不显示工具栏(系统行为)——改 Qt.Tool 不进
      任务栏;③ 图标配色误读 AppsUseLightTheme,预览页实随
      SystemUsesLightTheme(混搭主题下白上白/黑上黑不可见)——改读
      系统主题键;④ HICON 像素回读回归,防全透明隐形图标;回归 +4 例)

- [x] 动效打磨(借鉴 Android 端参数体系:FastOutSlowIn、进 220-300ms /
      出 120-250ms、缩放淡入过渡、防闪延迟):
      ① 播放/暂停按钮图标切换缩放+淡入过渡(A 端 AnimatedContent)
      ② 播放条封面切歌交叉淡入,不再闪占位图(A 端 900ms 防闪思路)
      ③ 播放条橡皮筋横滑切上一首/下一首(A 端 MiniPlayer 手势:
        指数阻尼+阈值+回弹)
      ④ 歌曲表「正在播放」行均衡器跳动条指示(A 端三根条,暂停回落)
      ⑤ 中央页面切换快出慢进滑动过渡(A 端 Tab 转场 70/330ms)
      (2026-09-26 实现:① AnimatedIconButton 200ms OutCubic 交叉缩放,
      同实例图标跳过防重放;② _CoverLabel 200ms 交叉淡入 + 900ms 延迟
      清空可被新封面取消;③ _SwipeHost 指数阻尼(峰值 52px/阈值 72px/
      弹回 200ms),按住封面+信息块横滑;④ 序号列委托画三根正弦条
      (周期 520/680/600ms 错相,~30fps 只重绘当前行,暂停 180ms 回落,
      行号随切歌/换表重算);⑤ _SlideStack 旧页截图 120ms 快出 +
      新页 300ms 慢进,过渡中重切即时收敛;回归 22 例覆盖阻尼数学/
      手势阈值/图标过渡/防闪/页面切换/均衡器状态机)

- [x] 任务栏歌名显示:起播后窗口标题(=任务栏文案)换为歌名,宽度
      规范到与「NeriPlayer Win」恒定一致——过长按 … 截断、过短补空格,
      任务栏按钮宽度不随歌名抖动;停止播放/播完队尾恢复基础标题
      (2026-09-26 实现:taskbar_title 以 QFontMetrics 量基准宽,超宽
      ElideRight 截断,不足按空格步进补齐(余量<一个空格);起播设置、
      媒体停止/顺序播完恢复,暂停与播放失败保持歌名;回归 7 例覆盖
      截断/补齐/CJK 宽度/生命周期)
      (2026-09-26 修复等宽无效:Win11 任务栏文本层(XAML/DirectWrite)
      折叠尾随普通空格,补齐改用 NBSP(U+00A0)——不折叠且渲染为等宽
      空白;回归同步更新)

- [x] 播放动态取色(对齐 Android):播放歌曲时从封面提取主色,整个
      界面随之柔和变色;关播/无封面/关开关回默认冻结色板
      (2026-09-25 实现,对照 reference/NeriPlayer-Android 调研:
      CoverArtColorCache 用 androidx Palette 在 96px 缩略图上
      vibrant→muted→dominant 择色,NeriTheme 用 materialkolor
      rememberDynamicColorScheme(TonalSpot)从种子生成整套 M3
      色板,dynamicColor 开关默认开、种子回落 #0061A4。
      本项目落地:ui/material_color.py 为 Google
      material-color-utilities(Apache-2.0,注明引用)HCT/CAM16/
      TonalPalette/SchemeTonalSpot 的最小 Python 移植,与上游
      Python 移植对拍 10 种子×亮暗×25 角色 500/500 一致,参考值
      烘干进 tests/test_dynamic_theme.py;ui/cover_seed.py 同语义
      近似 Palette 择色(5bit 量化直方图+HSL 打分,64 条 FIFO 缓存);
      ThemeManager.apply_dynamic 按当前明暗现算色板全量重渲(明暗
      模式名不变,托盘图标不受影响);设置页外观组新增「播放时跟随
      封面取色」开关(默认开,settings.json 增 dynamic_color 键);
      MainWindow 以独立 CoverLoader 取封面字节(磁盘缓存同源,零额外
      网络),代际 token 防快速切歌串色;回归 24 例+离屏渲染视觉
      验收(暖橙/紫粉/青绿三主题可读性通过)

## 明确暂缓(有需求再启动)

- 歌词(获取/展示/滚动)
- 音乐下载
- 一起听
- YouTube Music
- 本地音乐库导入
- 音效/均衡器

## 非功能要求(长期生效)

- 轻量:内存占用与启动时间是验收指标,不是事后优化
- 数据自持:登录态与配置只存本地,不引入任何云服务
- 合规:仅在本人已授权账号与平台规则允许范围内访问内容
